<% if !@followers.blank? %>
  <% @followers.each do |item| %>
    jQuery(".pagination").before('<%= escape_javascript(render("follower",:follower => item)) %>');
  <% end %>
  current_page = "<%= (params[:page] || "1").to_i + 1 %>";
  <% if @followers.count < @per_page %>
  jQuery(".pagination").remove();
  <% end %>
<% else %>
  jQuery(".pagination").remove();
<% end %>

