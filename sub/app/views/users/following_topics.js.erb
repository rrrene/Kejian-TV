<% if !@topics.blank? %>
  <% @topics.each do |item| %>
    jQuery(".pagination").before('<%= escape_javascript(render("topic",:topic => Topic.get_name(item))) %>');
  <% end %>
  current_page = "<%= (params[:page] || "1").to_i + 1 %>";
  <% if @topics.count < @per_page %>
  jQuery(".pagination").remove();
  <% end %>
<% else %>
  jQuery(".pagination").remove();
<% end %>

